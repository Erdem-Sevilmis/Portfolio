'use client';
import { HStack, Heading, VStack } from '@chakra-ui/react';
export default function MainPageStepper() {

    return (
        <>
            <p>Most recent proj. mit button und so</p>
        </>
    );
}
